# OS / editor metadata
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~

# Agent / local tooling
.claude/

# ── Credentials: never commit any of these ───────────────────
# The ElevenLabs key is used only by the OFFLINE tools in tools/.
# It must never reach the browser bundle or this repository.
tools/.elevenlabs_key
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
credentials.json
secrets.json
.netrc

# Build output — generated by tools/build_artifact.sh, never committed
_site/

# Local-only reports
security-report-*.txt
*.local.json

# Build output — generated by tools/build_artifact.sh, never committed
_site/
tools/voices.json

# Audio lives in the IPA-Audio repository (checked out beside this one)
# since 2026-09-21. Never re-add it here.
/audio/
